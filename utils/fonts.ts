import { Margarine, Old_Standard_TT } from "next/font/google";

export const textFont = Old_Standard_TT({
  weight: "400",
  subsets:["latin"]
});

export const titleFont = Margarine({
  weight: "400",
  subsets:["latin"]

});





